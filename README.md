# node-react-assignment
# first start node server
> go to /api
> npm install || yarn install
> npm start || yarn start

# start react app
> go to /frontend
> npm install || yarn install
> npm start || yarn start
